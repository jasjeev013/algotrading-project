const AdvancedSettingsSidebar = () => {
  return (
    <div className="live-panel">
      <span className="live-badge">GLOBAL DEFAULTS</span>

      <p className="live-note">
        Commission, spread, slippage, and overnight financing apply to every
        Strategy Explorer and Walk Forward Engine run — set once here, no
        per-run overrides.
      </p>
    </div>
  );
};

export default AdvancedSettingsSidebar;
